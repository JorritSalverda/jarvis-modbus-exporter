use std::process;

fn main() {
    println!("Hello, world!");

    // get params
    let host = String::from("192.168.1.1");
    let port = 502;
    let unit_id = 7;

    // let config = ModbusClientConfig::new(host, port, unit_id).unwrap_or_else(|err| {
    //   println!("Problem parsing arguments: {}", err);
    //   process::exit(1);
    // });


    // init logging

    // init config client

    // init bigquery client

    // init kube client

    // init state client

    // init modbus client

    // init exporter service

    // run exporter service

}
